import 'package:flutter/material.dart';

class EmployerNewpassword extends StatelessWidget {
  const EmployerNewpassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
