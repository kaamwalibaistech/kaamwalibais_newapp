import 'package:flutter/material.dart';

class BookmaidScreen extends StatelessWidget {
  const BookmaidScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(body: Center(child: Text("BookMaid")),);
  }
}